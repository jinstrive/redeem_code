#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.2)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:new_style
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import TSSLSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from redeem_code import RedeemCode
from redeem_code.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print('')
  print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] function [arg1 [arg2...]]')
  print('')
  print('Functions:')
  print('  string ping()')
  print('   create_codes(string bid, i32 quantity, i32 bits, i32 ctype, string mark)')
  print('  i32 load_codes(string bid,  codes)')
  print('  Code get_code(string bid, i32 gtype)')
  print('   get_codes(string bid, i32 quantity, i32 gtype)')
  print('  bool code_redeem(string bid, string code)')
  print('')
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
  ssl = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSSLSocket.TSSLSocket(host, port, validate=False) if ssl else TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = RedeemCode.Client(protocol)
transport.open()

if cmd == 'ping':
  if len(args) != 0:
    print('ping requires 0 args')
    sys.exit(1)
  pp.pprint(client.ping())

elif cmd == 'create_codes':
  if len(args) != 5:
    print('create_codes requires 5 args')
    sys.exit(1)
  pp.pprint(client.create_codes(args[0],eval(args[1]),eval(args[2]),eval(args[3]),args[4],))

elif cmd == 'load_codes':
  if len(args) != 2:
    print('load_codes requires 2 args')
    sys.exit(1)
  pp.pprint(client.load_codes(args[0],eval(args[1]),))

elif cmd == 'get_code':
  if len(args) != 2:
    print('get_code requires 2 args')
    sys.exit(1)
  pp.pprint(client.get_code(args[0],eval(args[1]),))

elif cmd == 'get_codes':
  if len(args) != 3:
    print('get_codes requires 3 args')
    sys.exit(1)
  pp.pprint(client.get_codes(args[0],eval(args[1]),eval(args[2]),))

elif cmd == 'code_redeem':
  if len(args) != 2:
    print('code_redeem requires 2 args')
    sys.exit(1)
  pp.pprint(client.code_redeem(args[0],args[1],))

else:
  print('Unrecognized method %s' % cmd)
  sys.exit(1)

transport.close()
